// *Used to reset the changes each class might have made* //
alias sens_aim				"sensitivity 0.275"
alias sens_def				"sensitivity 0.5"
alias sens_fst				"sensitivity 0.53"


unbind						"b"
unbind						"f"
unbind						"mouse5"
bind 1						"slot1"
bind 2						"slot2"
bind 3						"slot3"
bind 4						"slot4"
bind f						"+inspect"
bind e						"voicemenu 0 0"
bind r						"+reload" // from chatbinds
bind mouse5					"+attack2"
bind mouse2					"+jump"
bind mouse1					"+attack"
bind space					"+jump"
bind shift					"+duck"
-attack
loadout_0
//snd_restart
sens_def
fov_def